function try_login() {
	if (document.login) {
		let form = document.login;
		if (!form.username.value || !form.password.value) {
			
		}
		else {
			
		}
	}
}

function edit(element) {
	let id = element.id;
	
}